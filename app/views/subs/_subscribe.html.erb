<%= form_for(current_user.active_subscriptions.build, remote: true) do |f| %>
  <%= hidden_field_tag :sub_id, @sub.id %>
  <%= button_tag :class => "btn btn-primary subscribe" do %>
  	<span class="fa fa-plus-circle"></span> <%= t(:sub_subscribe) %>
  <% end %>
<% end %>