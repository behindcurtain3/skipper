$('#comment_<%= @comment.id %>_children').html('');

<% @comments.each do |comment| %>
$('#comment_<%= @comment.id %>_children').append("<%= escape_javascript(render 'comment', :comment => comment, :depth => 0, :maxdepth => 1) %>");
<% end %>
